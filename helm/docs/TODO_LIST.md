백엔드
1. 멀티스테이지 빌드 적용
2. health check 엔드포인트 구현
3. 환경별 values 파일 분리
클라우드
1. _helpers.tpl 에 공통 라벨, 이름 규칙 정의
2. Nginx Ingress -> Gateway API 전환 방법 확인
공-통
1. Docker Repository 설정
2. Helm Repository 설정
3. Istio, Gateway API 학습
4. AWS 계정 만들기

다 되면 AWS에 EKS 설정 + RDS(ExternalName)
