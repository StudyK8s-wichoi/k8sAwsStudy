apiVersion: v1
kind: Service
metadata:
  name: chapter
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "study.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: chapter
  ports:
    - port: 8080
      targetPort: 8080
