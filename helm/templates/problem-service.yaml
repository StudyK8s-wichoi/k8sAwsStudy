apiVersion: v1
kind: Service
metadata:
  name: problem
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "study.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: problem
  ports:
    - port: 8080
      targetPort: 8080
