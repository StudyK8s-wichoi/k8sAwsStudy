server:
  port: 8080
eureka:
  client:
    enabled: false
spring:
  cloud:
    openfeign:
      client:
        config:
          problem-service:
            url: http://problem.backend.svc.cluster.local:8080
